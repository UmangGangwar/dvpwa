            - name: Bandit Check
  # You may pin to the exact commit or the version.
  # uses: jpetrucciani/bandit-check@4e0dbcc188c035d2c4fb338a108ac93ed9d9d9be
  uses: jpetrucciani/bandit-check@1.7.4
  with:
    # File or directory to run bandit on
    path: # optional, default is .
    # Optional Bandit flags (refer to `bandit --help`)
    bandit_flags: # optional, default is 
    # 
    output: # optional
          
